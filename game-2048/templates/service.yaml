apiVersion: v1
kind: Service
metadata:
  name: 2048-service
spec:
  type: NodePort
  selector:
    app: 2048-game
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.containerPort }}
