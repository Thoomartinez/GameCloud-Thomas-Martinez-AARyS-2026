apiVersion: v1
kind: Service
metadata:
  name: doom-service
spec:
  type: NodePort
  selector:
    app: doom
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.containerPort }}
